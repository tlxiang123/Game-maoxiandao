1：ms打开背包 = &CColor{Name: "特征名" ，""里面的特征名，改成和它前面的变量一样的名字，如ms打开背包。
2  }后面的这个注释//，nt开头的就是不需要点击， 只需要找到特征就算成功， 而ck开头的需要点击对应的， 单独做2个按钮， 在第四层的时候， 如果我点买卖物品按钮，则开始，每次点击下一步这个按钮执行新动作则输出，否则不输出，输出结果如下：ms打开背包成功，找到对应打开背包成功特征， 或ms第五页面灰色失败， ck第五页面点开 后， 特征未变。